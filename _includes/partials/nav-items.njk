{% if navigation.items %}
    {% for item in navigation.items %}
        {% set externalAttribute = '' %}
        {% set currentAttribute = '' %}

        {% if item.external %}
            {% set externalAttribute = ' rel="external"' %}
        {% endif %}

        {% if page.url == item.url %}
            {% set currentAttribute = ' aria-current="page"' %}
        {% endif %}

        <a class="block md:inline-block py-2 rounded hover:text-gray-600" href="{{ site.baseURL }}{{ item.url }}" {{ externalAttribute | safe }} {{ currentAttribute | safe }}>
            {{ item.text }}
        </a>
    {% endfor %}
{% endif %}
