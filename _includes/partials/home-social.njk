{% if navigation.social %}
    {% for item in navigation.social %}
        {% set externalAttribute = '' %}
        {% set currentAttribute = '' %}
        {% set emailId = '' %}
        {% if item.external %}
            {% set externalAttribute = ' rel="external" target="blank"' %}
        {% endif %}
        {% if page.url == item.url %}
            {% set currentAttribute = ' aria-current="page"' %}
        {% endif %}
        <a class="inline-block py-2 rounded nav-item-color" href="{{ item.url }}" {{ externalAttribute | safe }} {{ currentAttribute | safe }} tabindex="1">
            <i class="text-lg mr-2 {{ item.icon }} fa-fw"></i>
        </a>
    {% endfor %}
{% endif %}
